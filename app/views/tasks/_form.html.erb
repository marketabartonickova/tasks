<%= form_for(@task) do |f| %>
  <% if @task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved: <% @task.errors.full_messages.each do |msg| %>
       <%= msg %></h2>
      <% end %>
      </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <p></p>
  <div class="field">
    <%= f.label :priority %><br>
    <%= f.select "priority", [1,2,3]%>
  </div>
  <p></p>
  <div class="field">
    <%= f.label :deadline %><br>
    <%= f.datetime_select :deadline %>
  </div>
  <p></p>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
